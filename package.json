{
  "name": "card-top-up-with-mimic",
  "private": true,
  "version": "1.0.0",
  "description": "Crypto card auto top-up example with Mimic",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/mocha",
      "**/mocha/**"
    ]
  }
}
